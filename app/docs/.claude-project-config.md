# Configuration Projet - Standards de Développement

## 🎯 Informations Projet

### Contexte
**Nom du projet :** FKS Facility
**Description :** Central de référencement
**Type :** Website
**Public cible :** Directeur achat généraux, Deirecteur logistique

### Stack Technique
- **Frontend :** React 18+ avec TypeScript
- **Backend :** Node.js 20+ avec Express
- **Base de données :** [COMPLETER: PostgreSQL/MySQL/MongoDB via Supabase/autre]
- **Paiement :** Stripe 20.0.0 backend et 8.5.3 frontend
- **ORM/Query Builder :** [COMPLETER: Prisma/Drizzle/Supabase Client]
- **Authentification :** [COMPLETER: Supabase Auth/Auth0/JWT custom]
- **Hosting :** [COMPLETER: Vercel/Railway/AWS]
- **State Management :** [COMPLETER: Zustand/Redux/TanStack Query/Context API]

---

## 🏗️ Architecture & Structure

### Organisation des dossiers

#### Frontend (React)
```
src/
├── assets/           # Images, fonts, fichiers statiques
├── components/       # Composants réutilisables
│   ├── ui/          # Composants UI génériques (Button, Input, etc.)
│   ├── forms/       # Composants de formulaires
│   └── layouts/     # Layouts (Header, Footer, Sidebar)
├── features/        # Fonctionnalités métier (chaque feature = dossier)
│   └── [feature]/
│       ├── components/   # Composants spécifiques à la feature
│       ├── hooks/        # Hooks custom de la feature
│       ├── services/     # Logique métier et API calls
│       ├── types/        # Types TypeScript
│       └── utils/        # Utilitaires de la feature
├── hooks/           # Hooks globaux réutilisables
├── lib/             # Configuration des librairies externes
├── pages/           # Pages/Routes principales
├── services/        # Services globaux (API client, analytics)
├── stores/          # State management global
├── types/           # Types TypeScript globaux
├── utils/           # Fonctions utilitaires globales
└── config/          # Configuration app (constants, env)
```

#### Backend (Node.js)
```
src/
├── config/          # Configuration (database, env, constants)
├── controllers/     # Contrôleurs (logique des routes)
├── middleware/      # Middlewares (auth, validation, error)
├── models/          # Modèles de données (Prisma/TypeORM)
├── routes/          # Définition des routes
├── services/        # Logique métier
├── utils/           # Fonctions utilitaires
├── types/           # Types TypeScript
├── validators/      # Schémas de validation (Zod/Joi)
└── tests/           # Tests unitaires et d'intégration
```

---

## ⚛️ Standards React

### Règles Fondamentales

1. **TOUJOURS utiliser TypeScript** - Aucun fichier .jsx, uniquement .tsx
2. **Composants fonctionnels uniquement** - Pas de class components
3. **Un composant = un fichier** - Sauf composants très petits et liés
4. **Props avec interface TypeScript** - Toujours typer les props

### Conventions de Nommage

```typescript
// ✅ CORRECT
interface UserCardProps {
  user: User;
  onEdit: (id: string) => void;
}

export const UserCard = ({ user, onEdit }: UserCardProps) => {
  // ...
}

// Fichier : UserCard.tsx
// Export nommé pour les composants
// PascalCase pour composants et types
```

```typescript
// ❌ INCORRECT
export default function userCard(props: any) { ... }
// - Pas de default export pour composants
// - Pas de camelCase pour composants
// - Pas de type any
```

### Structure d'un Composant

```typescript
// 1. Imports externes
import { useState, useEffect } from 'react';
import { useQuery } from '@tanstack/react-query';

// 2. Imports internes
import { Button } from '@/components/ui/Button';
import { useAuth } from '@/hooks/useAuth';

// 3. Imports types
import type { User } from '@/types/user';

// 4. Types/Interfaces
interface UserProfileProps {
  userId: string;
  onUpdate?: (user: User) => void;
}

// 5. Composant
export const UserProfile = ({ userId, onUpdate }: UserProfileProps) => {
  // 5.1 Hooks d'état
  const [isEditing, setIsEditing] = useState(false);
  
  // 5.2 Hooks custom
  const { user: currentUser } = useAuth();
  
  // 5.3 Queries/Mutations
  const { data: user, isLoading } = useQuery({
    queryKey: ['user', userId],
    queryFn: () => fetchUser(userId),
  });
  
  // 5.4 Effects
  useEffect(() => {
    // ...
  }, [userId]);
  
  // 5.5 Event handlers
  const handleEdit = () => {
    setIsEditing(true);
  };
  
  // 5.6 Early returns
  if (isLoading) return <LoadingSpinner />;
  if (!user) return <ErrorMessage />;
  
  // 5.7 Render
  return (
    <div className="user-profile">
      {/* ... */}
    </div>
  );
};
```

### Hooks - Bonnes Pratiques

```typescript
// Hook custom - Préfixe "use"
export const useUserData = (userId: string) => {
  const [data, setData] = useState<User | null>(null);
  const [error, setError] = useState<Error | null>(null);
  const [isLoading, setIsLoading] = useState(true);
  
  // Toujours nettoyer les effects
  useEffect(() => {
    let cancelled = false;
    
    const fetchData = async () => {
      try {
        const result = await fetchUser(userId);
        if (!cancelled) {
          setData(result);
        }
      } catch (err) {
        if (!cancelled) {
          setError(err as Error);
        }
      } finally {
        if (!cancelled) {
          setIsLoading(false);
        }
      }
    };
    
    fetchData();
    
    return () => {
      cancelled = true;
    };
  }, [userId]);
  
  return { data, error, isLoading };
};
```

### Gestion d'État

**[COMPLETER: Quelle approche privilégier pour ton projet]**

#### Option 1 : TanStack Query (React Query) pour les données serveur
```typescript
// Préférer React Query pour tout ce qui vient du serveur
const { data, isLoading, error } = useQuery({
  queryKey: ['users', filters],
  queryFn: () => api.users.list(filters),
  staleTime: 5 * 60 * 1000, // 5 minutes
});

// Mutations
const mutation = useMutation({
  mutationFn: api.users.create,
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ['users'] });
  },
});
```

#### Option 2 : Zustand pour l'état UI global
```typescript
// stores/useAppStore.ts
interface AppStore {
  sidebarOpen: boolean;
  toggleSidebar: () => void;
}

export const useAppStore = create<AppStore>((set) => ({
  sidebarOpen: true,
  toggleSidebar: () => set((state) => ({ 
    sidebarOpen: !state.sidebarOpen 
  })),
}));
```

### Performance

```typescript
// ✅ Mémoïser les calculs coûteux
const expensiveValue = useMemo(() => {
  return complexCalculation(data);
}, [data]);

// ✅ Mémoïser les callbacks passés en props
const handleClick = useCallback(() => {
  doSomething(id);
}, [id]);

// ✅ Lazy loading des composants lourds
const HeavyComponent = lazy(() => import('./HeavyComponent'));

// ✅ Code splitting par route
const Dashboard = lazy(() => import('@/pages/Dashboard'));
```

---

## 🚀 Standards Node.js / Backend

### Architecture en Couches

```
Route → Controller → Service → Model → Database
```

**TOUJOURS respecter cette séparation :**
- **Routes :** Définition des endpoints uniquement
- **Controllers :** Validation des inputs, gestion des réponses HTTP
- **Services :** Logique métier
- **Models :** Interaction avec la base de données

### Exemple Complet

```typescript
// routes/users.routes.ts
import { Router } from 'express';
import { userController } from '@/controllers/user.controller';
import { authenticate } from '@/middleware/auth';
import { validateRequest } from '@/middleware/validation';
import { createUserSchema } from '@/validators/user.validator';

const router = Router();

router.get('/users', authenticate, userController.list);
router.post(
  '/users',
  authenticate,
  validateRequest(createUserSchema),
  userController.create
);

export default router;
```

```typescript
// controllers/user.controller.ts
import { Request, Response, NextFunction } from 'express';
import { userService } from '@/services/user.service';
import { ApiError } from '@/utils/ApiError';

export const userController = {
  list: async (req: Request, res: Response, next: NextFunction) => {
    try {
      const { page = 1, limit = 10 } = req.query;
      const result = await userService.listUsers({
        page: Number(page),
        limit: Number(limit),
        organizationId: req.user!.organizationId, // Multi-tenant
      });
      
      res.json({
        success: true,
        data: result.users,
        pagination: result.pagination,
      });
    } catch (error) {
      next(error);
    }
  },
  
  create: async (req: Request, res: Response, next: NextFunction) => {
    try {
      const user = await userService.createUser({
        ...req.body,
        organizationId: req.user!.organizationId,
      });
      
      res.status(201).json({
        success: true,
        data: user,
      });
    } catch (error) {
      next(error);
    }
  },
};
```

```typescript
// services/user.service.ts
import { prisma } from '@/lib/prisma';
import { ApiError } from '@/utils/ApiError';
import type { CreateUserInput, ListUsersInput } from '@/types/user';

export const userService = {
  listUsers: async (input: ListUsersInput) => {
    const { page, limit, organizationId } = input;
    const skip = (page - 1) * limit;
    
    const [users, total] = await Promise.all([
      prisma.user.findMany({
        where: { organizationId },
        skip,
        take: limit,
        orderBy: { createdAt: 'desc' },
      }),
      prisma.user.count({ where: { organizationId } }),
    ]);
    
    return {
      users,
      pagination: {
        page,
        limit,
        total,
        totalPages: Math.ceil(total / limit),
      },
    };
  },
  
  createUser: async (input: CreateUserInput) => {
    // Vérifier l'unicité de l'email dans l'organisation
    const existing = await prisma.user.findFirst({
      where: {
        email: input.email,
        organizationId: input.organizationId,
      },
    });
    
    if (existing) {
      throw new ApiError(409, 'User already exists');
    }
    
    return prisma.user.create({
      data: input,
    });
  },
};
```

### Gestion des Erreurs

```typescript
// utils/ApiError.ts
export class ApiError extends Error {
  constructor(
    public statusCode: number,
    public message: string,
    public isOperational = true
  ) {
    super(message);
    Object.setPrototypeOf(this, ApiError.prototype);
  }
}

// middleware/errorHandler.ts
export const errorHandler = (
  err: Error,
  req: Request,
  res: Response,
  next: NextFunction
) => {
  if (err instanceof ApiError) {
    return res.status(err.statusCode).json({
      success: false,
      message: err.message,
    });
  }
  
  // Log les erreurs non prévues
  console.error('Unhandled error:', err);
  
  return res.status(500).json({
    success: false,
    message: 'Internal server error',
  });
};
```

### Validation

```typescript
// validators/user.validator.ts
import { z } from 'zod';

export const createUserSchema = z.object({
  body: z.object({
    email: z.string().email('Invalid email'),
    name: z.string().min(2, 'Name too short'),
    role: z.enum(['admin', 'user']),
  }),
});

export const updateUserSchema = z.object({
  params: z.object({
    id: z.string().uuid('Invalid user ID'),
  }),
  body: z.object({
    name: z.string().min(2).optional(),
    role: z.enum(['admin', 'user']).optional(),
  }),
});

// middleware/validation.ts
import { AnyZodObject } from 'zod';

export const validateRequest = (schema: AnyZodObject) => {
  return async (req: Request, res: Response, next: NextFunction) => {
    try {
      await schema.parseAsync({
        body: req.body,
        query: req.query,
        params: req.params,
      });
      next();
    } catch (error) {
      if (error instanceof z.ZodError) {
        return res.status(400).json({
          success: false,
          message: 'Validation error',
          errors: error.errors,
        });
      }
      next(error);
    }
  };
};
```

---

## 🔒 Sécurité

### Multi-tenant - Isolation des Données

**RÈGLE ABSOLUE :** Toujours filtrer par `organizationId` dans TOUTES les requêtes.

```typescript
// ✅ CORRECT - Toujours inclure organizationId
const user = await prisma.user.findFirst({
  where: {
    id: userId,
    organizationId: req.user!.organizationId, // 🔒 CRITIQUE
  },
});

// ❌ INCORRECT - Risque de fuite de données cross-tenant
const user = await prisma.user.findFirst({
  where: { id: userId },
});
```

### Middleware d'Authentification

```typescript
// middleware/auth.ts
import { Request, Response, NextFunction } from 'express';
import { verifyToken } from '@/lib/auth';
import { ApiError } from '@/utils/ApiError';

declare global {
  namespace Express {
    interface Request {
      user?: {
        id: string;
        organizationId: string;
        role: string;
      };
    }
  }
}

export const authenticate = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {
  try {
    const token = req.headers.authorization?.replace('Bearer ', '');
    
    if (!token) {
      throw new ApiError(401, 'Authentication required');
    }
    
    const payload = await verifyToken(token);
    req.user = payload;
    
    next();
  } catch (error) {
    next(new ApiError(401, 'Invalid or expired token'));
  }
};

export const authorize = (...roles: string[]) => {
  return (req: Request, res: Response, next: NextFunction) => {
    if (!req.user || !roles.includes(req.user.role)) {
      throw new ApiError(403, 'Insufficient permissions');
    }
    next();
  };
};
```

### Variables d'Environnement

```typescript
// config/env.ts
import { z } from 'zod';

const envSchema = z.object({
  NODE_ENV: z.enum(['development', 'production', 'test']),
  PORT: z.string().transform(Number),
  DATABASE_URL: z.string().url(),
  JWT_SECRET: z.string().min(32),
  // [COMPLETER: Ajouter tes variables d'environnement]
});

export const env = envSchema.parse(process.env);
```

---

## 📝 Conventions de Code

### Nommage

```typescript
// Variables et fonctions : camelCase
const userName = 'John';
const fetchUserData = () => {};

// Constantes : UPPER_SNAKE_CASE
const MAX_RETRY_COUNT = 3;
const API_BASE_URL = 'https://api.example.com';

// Classes et Types : PascalCase
class UserService {}
interface UserProfile {}
type ApiResponse<T> = { data: T };

// Fichiers :
// - Composants : PascalCase (UserCard.tsx)
// - Hooks : camelCase avec use (useAuth.ts)
// - Utils : camelCase (formatDate.ts)
// - Services : camelCase avec .service (user.service.ts)
```

### Commentaires

```typescript
// ❌ Éviter les commentaires évidents
// Increment counter by 1
counter++;

// ✅ Expliquer le "pourquoi", pas le "quoi"
// Delayed by 100ms to avoid rate limiting
await delay(100);

// ✅ JSDoc pour les fonctions publiques
/**
 * Fetches user data with caching
 * @param userId - The unique user identifier
 * @param options - Optional fetch configuration
 * @returns Promise resolving to user data
 * @throws {ApiError} When user is not found
 */
export const fetchUser = async (
  userId: string,
  options?: FetchOptions
): Promise<User> => {
  // ...
};
```

### Imports

```typescript
// ✅ Organisation des imports
// 1. External libraries
import React, { useState } from 'react';
import { useQuery } from '@tanstack/react-query';

// 2. Internal absolute imports
import { Button } from '@/components/ui/Button';
import { useAuth } from '@/hooks/useAuth';

// 3. Types
import type { User } from '@/types/user';

// 4. Relative imports (si vraiment nécessaire)
import { helper } from './utils';

// ❌ Éviter les imports relatifs profonds
import { helper } from '../../../utils/helper';
// Utiliser plutôt des alias : import { helper } from '@/utils/helper';
```

---

## 🧪 Tests

### Stratégie de Tests

**[COMPLETER: Quelle approche de tests privilégier]**

```typescript
// Frontend : Vitest + React Testing Library
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { UserCard } from './UserCard';

describe('UserCard', () => {
  it('should display user name', () => {
    const user = { id: '1', name: 'John Doe' };
    render(<UserCard user={user} />);
    
    expect(screen.getByText('John Doe')).toBeInTheDocument();
  });
  
  it('should call onEdit when edit button is clicked', () => {
    const onEdit = vi.fn();
    const user = { id: '1', name: 'John Doe' };
    
    render(<UserCard user={user} onEdit={onEdit} />);
    fireEvent.click(screen.getByRole('button', { name: /edit/i }));
    
    expect(onEdit).toHaveBeenCalledWith('1');
  });
});
```

```typescript
// Backend : Jest/Vitest
import { describe, it, expect, beforeEach } from 'vitest';
import { userService } from './user.service';
import { prismaMock } from '@/lib/prisma.mock';

describe('UserService', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });
  
  describe('createUser', () => {
    it('should create user successfully', async () => {
      const input = {
        email: 'test@example.com',
        name: 'Test User',
        organizationId: 'org-1',
      };
      
      prismaMock.user.findFirst.mockResolvedValue(null);
      prismaMock.user.create.mockResolvedValue({
        id: 'user-1',
        ...input,
      });
      
      const result = await userService.createUser(input);
      
      expect(result).toMatchObject(input);
      expect(prismaMock.user.create).toHaveBeenCalledWith({
        data: input,
      });
    });
    
    it('should throw error if user already exists', async () => {
      prismaMock.user.findFirst.mockResolvedValue({
        id: 'existing-user',
        email: 'test@example.com',
      } as any);
      
      await expect(
        userService.createUser({
          email: 'test@example.com',
          name: 'Test',
          organizationId: 'org-1',
        })
      ).rejects.toThrow('User already exists');
    });
  });
});
```

---

## 📊 Base de Données

### Conventions Prisma

```prisma
// schema.prisma

model Organization {
  id        String   @id @default(cuid())
  name      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  users     User[]
  
  @@map("organizations")
}

model User {
  id             String   @id @default(cuid())
  email          String
  name           String
  role           Role     @default(USER)
  organizationId String
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
  
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  
  @@unique([email, organizationId])
  @@index([organizationId])
  @@map("users")
}

enum Role {
  ADMIN
  USER
}
```

**Règles :**
- `id` : Toujours CUID ou UUID
- `createdAt`, `updatedAt` : Sur toutes les tables
- `@@map("table_name")` : Noms de tables en snake_case
- Index sur les foreign keys et champs fréquemment filtrés
- `onDelete: Cascade` pour multi-tenant (supprimer org = supprimer users)

---

## 🎨 Styling

**[COMPLETER: Quelle approche CSS utiliser]**

### Option 1 : Tailwind CSS
```typescript
// ✅ Utiliser des classes utilitaires
<button className="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors">
  Click me
</button>

// ✅ Extraire les patterns répétés en composants
<Button variant="primary" size="md">Click me</Button>
```

### Option 2 : CSS Modules
```typescript
// Button.module.css
.button {
  padding: 0.5rem 1rem;
  border-radius: 0.5rem;
  transition: background-color 0.2s;
}

.primary {
  background-color: #3b82f6;
  color: white;
}

// Button.tsx
import styles from './Button.module.css';

export const Button = ({ variant = 'primary' }) => (
  <button className={`${styles.button} ${styles[variant]}`}>
    Click me
  </button>
);
```

---

## 🚢 Déploiement & CI/CD

### Variables d'Environnement Requises

**[COMPLETER: Liste de toutes les variables d'env nécessaires]**

```bash
# Backend (.env)
NODE_ENV=production
PORT=3000
DATABASE_URL=postgresql://...
JWT_SECRET=your-secret-key-min-32-chars
CORS_ORIGIN=https://your-frontend.com

# Frontend (.env)
VITE_API_URL=https://api.your-domain.com
VITE_SUPABASE_URL=...
VITE_SUPABASE_ANON_KEY=...
```

### Scripts Package.json

```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "lint:fix": "eslint . --ext ts,tsx --fix",
    "type-check": "tsc --noEmit",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage"
  }
}
```

---

## 📚 Documentation Code

### Quand Documenter

```typescript
// ✅ Documenter :
// - Fonctions publiques/exportées
// - Logique métier complexe
// - Comportements non-évidents
// - APIs et interfaces

/**
 * Calculates the prorated subscription cost based on usage
 * 
 * Uses a complex algorithm that accounts for:
 * - Partial month usage
 * - Volume-based discounts
 * - Regional pricing differences
 * 
 * @param subscription - The active subscription
 * @param usageData - Usage metrics for the period
 * @returns The calculated cost in cents
 */
export const calculateProratedCost = (
  subscription: Subscription,
  usageData: UsageData
): number => {
  // ...
};

// ❌ Ne pas documenter :
// - Code évident
// - Getters/setters simples
// - Fonctions privées évidentes
```

---

## 🔧 Configuration ESLint & Prettier

```json
// .eslintrc.json
{
  "extends": [
    "eslint:recommended",
    "plugin:@typescript-eslint/recommended",
    "plugin:react/recommended",
    "plugin:react-hooks/recommended"
  ],
  "rules": {
    "@typescript-eslint/no-explicit-any": "error",
    "@typescript-eslint/explicit-function-return-type": "warn",
    "react/react-in-jsx-scope": "off",
    "react/prop-types": "off",
    "no-console": ["warn", { "allow": ["warn", "error"] }]
  }
}
```

```json
// .prettierrc
{
  "semi": true,
  "trailingComma": "es5",
  "singleQuote": true,
  "printWidth": 80,
  "tabWidth": 2,
  "arrowParens": "always"
}
```

---

## ⚡ Performance - Checklist

### Frontend
- [ ] Lazy loading des routes
- [ ] Code splitting par feature
- [ ] Mémoïsation des calculs lourds (useMemo)
- [ ] Mémoïsation des callbacks (useCallback)
- [ ] Optimisation des images (next/image ou vite-imagetools)
- [ ] React Query avec staleTime approprié
- [ ] Debounce sur les inputs de recherche
- [ ] Virtual scrolling pour grandes listes (react-window)

### Backend
- [ ] Index database sur colonnes fréquemment filtrées
- [ ] Pagination sur toutes les listes
- [ ] Sélection des champs (select) pour réduire payload
- [ ] Cache Redis pour données fréquentes
- [ ] Rate limiting par endpoint
- [ ] Connection pooling database
- [ ] Compression gzip/brotli
- [ ] CDN pour assets statiques

---

## 🐛 Debugging

### Logging

```typescript
// utils/logger.ts
import pino from 'pino';

export const logger = pino({
  level: process.env.LOG_LEVEL || 'info',
  transport: {
    target: 'pino-pretty',
    options: {
      colorize: true,
    },
  },
});

// Usage
logger.info({ userId, action: 'login' }, 'User logged in');
logger.error({ err, userId }, 'Failed to create user');
```

---

## ✅ Checklist Avant Commit

- [ ] Code lint sans erreurs (`npm run lint`)
- [ ] Types TypeScript valides (`npm run type-check`)
- [ ] Tests passent (`npm run test`)
- [ ] Pas de `console.log` oubliés
- [ ] Pas de `any` TypeScript
- [ ] Variables d'env documentées
- [ ] Isolation multi-tenant vérifiée (organizationId partout)
- [ ] Messages de commit descriptifs

---

## 🎯 Principes Directeurs

### TOUJOURS
- ✅ TypeScript strict partout
- ✅ Séparer logique métier et présentation
- ✅ Isolation multi-tenant (organizationId)
- ✅ Gestion d'erreur cohérente
- ✅ Validation des inputs
- ✅ Code DRY (Don't Repeat Yourself)

### JAMAIS
- ❌ Type `any` en TypeScript
- ❌ Logique métier dans les composants React
- ❌ Requêtes DB sans filtre organizationId
- ❌ Mots de passe en clair
- ❌ Données sensibles dans le frontend
- ❌ Code dupliqué

---

## 📞 Contacts & Ressources

**[COMPLETER: Informations utiles pour le projet]**

- **Repository :** [GitHub URL]
- **Documentation API :** [URL Swagger/Postman]
- **Design System :** [Figma URL]
- **Base de données :** [Supabase Dashboard URL]
- **Monitoring :** [Sentry/DataDog URL]

---

*Document vivant - Dernière mise à jour : [DATE]*
